/**
 * Función de salida de datos genérica.
 * @param {string} mensaje 
 * @returns {string} - Devuelve un mensaje originado en otra parte del código.
 */
 function salida(mensaje) {
    /**
     * Mensaje de salida.
     * @type {*}
     */
    var mensaje_salida = alert(mensaje)
    return mensaje_salida
}